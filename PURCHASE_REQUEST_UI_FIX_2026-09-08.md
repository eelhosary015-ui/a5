# Purchase Request UI Upgrade – 2026-09-08

تم تحسين شاشة «إنشاء طلب شراء» وبند «الأصناف المطلوبة» بدون تغيير منطق حفظ الطلبات أو جداول قاعدة البيانات.

## التعديلات
- تكبير نافذة إنشاء طلب شراء إلى عرض أكبر وارتفاع يصل إلى 95% من الشاشة.
- تكبير المسافات الداخلية وتحسين وضوح الحقول.
- إصلاح مشكلة اختفاء قائمة البحث عن الأصناف بسبب `overflow-hidden` في حاوية بند الأصناف.
- تحسين البحث ليعمل بالاسم والكود والباركود.
- توسيع قائمة نتائج البحث وإظهار:
  - الكود.
  - اسم الصنف.
  - الوحدة.
  - الرصيد الحالي.
  - متوسط التكلفة.
  - آخر سعر شراء.
- استخدام `last_purchase_price` كأولوية للسعر التقديري عند إضافة الصنف، ثم `avg_cost`/`cost` كبديل.
- توسيع جدول الأصناف المطلوبة وإضافة عمود آخر سعر شراء.
- عرض وصف الصنف المختصر داخل الصف عند توفره.
- تحسين حقول الكمية والسعر وأزرار الحذف.
- زيادة ارتفاع قائمة SearchableSelect إلى 28rem لتحسين استعراض الأصناف.

## الملفات المعدلة
- `src/components/Purchases.tsx`
- `src/components/SearchableSelect.tsx`

## التحقق
تم إجراء فحص TypeScript للملفات المعدلة. لم تظهر أخطاء syntax/parse خاصة بالتعديلات؛ تعذر إكمال build كامل لأن اعتماديات npm غير موجودة محليًا في بيئة الفحص ولم يكتمل `npm install` ضمن مهلة التنفيذ.

                  {/* Main Search Input */}
                  <div className="flex w-full lg:w-[360px] bg-white border border-slate-300 rounded overflow-hidden focus-within:border-blue-500 focus-within:ring-1 focus-within:ring-blue-500 transition-colors shadow-sm">
                    <input
                      type="text"
                      value={searchQuery}
                      onChange={(e) => setSearchQuery(e.target.value)}
                      placeholder="بيانات شخصية - رقم قومي - تليفون - اسم"
                      className="flex-1 px-4 py-2 outline-none text-xs bg-white text-right font-medium placeholder:text-slate-400"
                      dir="rtl"
                    />
                    <button className="bg-blue-500 px-3 flex items-center justify-center text-white hover:bg-blue-600 transition-colors shrink-0">
                      <Search className="w-4 h-4" />
                    </button>
                  </div>

                  {/* Department Filter Dropdown */}
                  <div className="flex w-full lg:w-48 bg-white border border-slate-300 rounded overflow-hidden focus-within:border-blue-500 focus-within:ring-1 focus-within:ring-blue-500 transition-colors shadow-sm">
                    <select
                      value={filterDepartmentId}
                      onChange={(e) => setFilterDepartmentId(e.target.value ? Number(e.target.value) : "")}
                      className="w-full px-3 py-2 outline-none text-xs bg-white text-right font-bold text-slate-700 cursor-pointer"
--
              <div className="p-6 border-b border-slate-100 flex items-center justify-between">
                <div className="flex items-center gap-3">
                  <div className="w-10 h-10 bg-emerald-100 rounded-2xl flex items-center justify-center">
                    <Download className="w-5 h-5 text-emerald-600" />
                  </div>
                  <div>
                    <h2 className="text-lg font-bold text-slate-900">استيراد موظفين من Excel</h2>
                    <p className="text-xs text-slate-500">ارفع ملف Excel (.xlsx / .xls) يحتوي على بيانات الموظفين</p>
                  </div>
                </div>
                <button onClick={() => { setShowImportModal(false); setImportFile(null); setImportPreview([]); setImportResult(null); }} className="p-2 hover:bg-slate-100 rounded-xl">
                  <X className="w-5 h-5" />
                </button>
              </div>

              <div className="p-6 space-y-4">
                <div className="bg-amber-50 border border-amber-200 rounded-2xl p-4 text-sm text-amber-800">
                  <p className="font-bold mb-1">الأعمدة المطلوبة في ملف Excel:</p>
                  <p>الاسم (name) - الوظيفة (job_title) - القسم (department_name) - الفرع (branch_name) - الراتب الأساسي (basic_salary) - كود الموظف (employee_code) - الهاتف (phone)</p>
                  <p className="mt-1 text-amber-600">الأعمدة الاختيارية: البريد (email) - العنوان (address) - نوع التعاقد (contract_type) - تاريخ التعيين (hire_date)</p>
                </div>
--
              </div>

              <div className="p-4 bg-slate-50 flex gap-2 rounded-b-3xl">
                <button
                  onClick={handleImportEmployees}
                  disabled={!importFile || importing}
                  className="flex-1 bg-emerald-600 hover:bg-emerald-700 disabled:bg-slate-300 text-white font-bold py-2.5 rounded-xl text-sm transition-colors"
                >
                  {importing ? "جاري الاستيراد..." : "استيراد الموظفين"}
                </button>
                <button onClick={() => { setShowImportModal(false); setImportFile(null); setImportPreview([]); setImportResult(null); }} className="px-6 bg-white border border-slate-200 hover:bg-slate-50 text-slate-600 py-2.5 rounded-xl text-sm font-bold transition-colors">
                  إلغاء
                </button>
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>

      {/* ========== EMPLOYEE DOCUMENTS MODAL ========== */}
      <AnimatePresence>
--
                <div className="flex items-center gap-3">
                  <div className="w-10 h-10 bg-blue-100 rounded-2xl flex items-center justify-center">
                    <FileCog className="w-5 h-5 text-blue-600" />
                  </div>
                  <div>
                    <h2 className="text-lg font-bold text-slate-900">ملفات ومستندات الموظف</h2>
                    <p className="text-xs text-slate-500">{documentsEmployee.name} - {(documentsEmployee as any).employee_code || `#${documentsEmployee.id}`}</p>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <button onClick={() => setShowDocUploadModal(true)} className="flex items-center gap-1 px-3 py-1.5 bg-blue-600 hover:bg-blue-700 text-white rounded-xl text-sm font-bold transition-colors">
                    <Plus className="w-4 h-4" />
                    رفع مستند
                  </button>
                  <button onClick={() => { setShowDocumentsModal(false); setDocumentsEmployee(null); }} className="p-2 hover:bg-slate-100 rounded-xl">
                    <X className="w-5 h-5" />
                  </button>
                </div>
              </div>

              <div className="p-6">
                {documentsLoading ? (
                  <div className="flex justify-center py-8"><div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-500" /></div>
                ) : employeeDocuments.length === 0 ? (
                  <div className="text-center py-12 text-slate-400">
--
                                href={doc.file_url}
                                target="_blank"
                                rel="noreferrer"
                                className="p-1.5 hover:bg-slate-100 rounded-lg text-slate-600"
                                title="عرض ومعاينة المستند"
                              >
                                <Eye className="w-4 h-4" />
                              </a>
                            </div>
                          )}
                          <button onClick={() => handleDeleteDocument(doc.id)} className="p-1.5 hover:bg-red-50 rounded-lg text-red-400 hover:text-red-600">
                            <Trash2 className="w-4 h-4" />
                          </button>
                        </div>
                      </div>
                    ))}
                  </div>
                )}
              </div>
            </motion.div>
          </motion.div>
--
          >
            <motion.div
              initial={{ scale: 0.95, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              exit={{ scale: 0.95, opacity: 0 }}
              className="bg-white rounded-3xl shadow-2xl w-full max-w-md"
              onClick={(e) => e.stopPropagation()}
            >
              <div className="p-6 border-b border-slate-100 flex items-center justify-between">
                <h2 className="text-lg font-bold">رفع مستند جديد</h2>
                <button onClick={() => setShowDocUploadModal(false)} className="p-2 hover:bg-slate-100 rounded-xl"><X className="w-5 h-5" /></button>
              </div>
              <form onSubmit={(e) => { e.preventDefault(); handleUploadDocument(); }} className="p-6 space-y-4">
                <div className="space-y-2">
                  <label className="text-sm text-slate-500">نوع المستند</label>
                  <select value={docForm.document_type} onChange={(e) => setDocForm({ ...docForm, document_type: e.target.value })} className="w-full bg-slate-50 border border-slate-200 rounded-2xl px-4 py-3 text-slate-900 focus:outline-none focus:border-blue-500">
                    <option>بطاقة شخصية</option>
                    <option>جواز سفر</option>
                    <option>شهادة مؤهل</option>
                    <option>شهادة خبرة</option>
                    <option>عقد عمل</option>
--
                </div>
                <div className="space-y-2">
                  <label className="text-sm text-slate-500">رفع الملف</label>
                  <input type="file" onChange={(e) => setDocUploadFile(e.target.files?.[0] || null)} className="w-full bg-slate-50 border border-slate-200 rounded-2xl px-4 py-3 text-slate-900 file:ml-2 file:py-1 file:px-3 file:rounded-lg file:border-0 file:text-sm file:bg-blue-50 file:text-blue-700" />
                </div>
                <div className="space-y-2">
                  <label className="text-sm text-slate-500">ملاحظات</label>
                  <textarea value={docForm.notes} onChange={(e) => setDocForm({ ...docForm, notes: e.target.value })} rows={2} className="w-full bg-slate-50 border border-slate-200 rounded-2xl px-4 py-3 text-slate-900 focus:outline-none focus:border-blue-500 resize-none" />
                </div>
                <div className="flex gap-2 pt-2">
                  <button type="submit" className="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-bold py-2.5 rounded-xl text-sm transition-colors">رفع المستند</button>
                  <button type="button" onClick={() => setShowDocUploadModal(false)} className="px-6 bg-white border border-slate-200 hover:bg-slate-50 text-slate-600 py-2.5 rounded-xl text-sm font-bold transition-colors">إلغاء</button>
                </div>
              </form>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>

      {/* Set Employee Mobile Password Modal */}
      {passwordModalEmp && (
        <SetEmployeePasswordModal

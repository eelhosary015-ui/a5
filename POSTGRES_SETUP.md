# ربط النظام بقاعدة بيانات PostgreSQL

لقد قمت بتجهيز النظام ليدعم كلاً من SQLite و PostgreSQL. لربط النظام بقاعدة بيانات PostgreSQL الخاصة بك، اتبع الخطوات التالية:

### 1. تجهيز ملف الإعدادات (`.env`)
قم بإنشاء ملف باسم `.env` في المجلد الرئيسي للمشروع (بجانب `package.json`) وأضف فيه الإعدادات التالية:

```env
DATABASE_TYPE=postgres
DATABASE_URL=postgres://اسم_المستخدم:كلمة_المرور@عنوان_IP_للسيرفر:5432/اسم_قاعدة_البيانات
DB_SSL=false
JWT_SECRET=اكتب_هنا_كلمة_سر_عشوائية_للتشفير
```

*ملاحظة: إذا كنت تربط النظام بسيرفر على نفس الشبكة (LAN)، استخدم عنوان IP الخاص بالسيرفر بدلاً من `localhost`. وتأكد من ضبط `DB_SSL=false` إذا لم يكن السيرفر يدعم التشفير.*

### 2. إعدادات خادم PostgreSQL للربط عبر الشبكة (LAN)
للسماح بالاتصال من أجهزة أخرى على الشبكة، يجب تعديل ملفات إعدادات PostgreSQL على جهاز السيرفر:

1. **ملف `postgresql.conf`:**
   ابحث عن السطر التالي وقم بتعديله ليصبح:
   ```conf
   listen_addresses = '*'
   ```

2. **ملف `pg_hba.conf`:**
   أضف سطراً في نهاية الملف للسماح بالاتصال من نطاق الشبكة الخاص بك:
   ```conf
   host    all             all             192.168.1.0/24          md5
   ```
   *(استبدل `192.168.1.0/24` بنطاق الشبكة الخاص بك)*

3. **جدار الحماية (Firewall):**
   تأكد من فتح المنفذ `5432` في جدار الحماية على جهاز السيرفر للسماح بالمرور.

### 2. تثبيت المكتبات اللازمة
تأكد من تثبيت مكتبة `pg` (لقد قمت بإضافتها بالفعل في `package.json`):
```bash
npm install
```

### 3. تشغيل النظام
عند تشغيل النظام باستخدام `npm run dev` أو `npm start` سيقوم النظام تلقائياً بالتعرف على نوع قاعدة البيانات وإنشاء الجداول اللازمة في PostgreSQL.

### 4. استعادة البيانات (اختياري)
إذا كان لديك نسخة احتياطية سابقة من النظام (ملف JSON)، يمكنك استعادتها من خلال لوحة الإدارة -> مدير قاعدة البيانات. النظام سيقوم تلقائياً بتحويل البيانات لتناسب PostgreSQL.

---

### مميزات التحويل التلقائي:
- النظام يقوم بتحويل أوامر SQLite (مثل `AUTOINCREMENT` و `DATETIME`) إلى ما يقابلها في PostgreSQL.
- يتم التعامل مع الـ `lastInsertRowid` بشكل تلقائي لضمان عمل واجهة المستخدم بدون أخطاء.
- يدعم النظام إنشاء الجداول تلقائياً عند أول تشغيل.

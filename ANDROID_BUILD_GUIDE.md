# دليل بناء وتحويل تطبيق Remo Pro إلى تطبيق أندرويد حقيقي (APK & AAB) للرفع على Google Play

مرحباً بك! لقد قمنا بتهيئة نظامك بالكامل لدعم تحويل تطبيق الويب الخاص بـ **Remo Pro** إلى تطبيق أندرويد متكامل باستخدام **CapacitorJS** (التقنية الأحدث والأقوى من Ionic والمدعومة رسمياً من Google لإنتاج تطبيقات Android عالية الأداء).

---

## 🚀 الخيارات المتاحة لك لتحويل التطبيق إلى Android

يحتوي مشروعك حالياً على طريقتين مدمجتين جاهزتين للعمل:

### الخيار الأول: استخدام تطبيق الويب التقدمي (PWA) - مدمج مسبقاً جاهز
التطبيق يدعم PWA بالكامل عبر `vite-plugin-pwa` المدمج. 
* **الميزة:** يمكن للموظفين والعملاء فتح الرابط والضغط على **"إضافة إلى الشاشة الرئيسية"** ليظهر التطبيق كأيقونة حقيقية على هاتفهم بدون تنزيل من المتجر.
* **للرفع على Google Play:** يمكنك استخدام أداة **Bubblewrap CLI** أو موقع **PWABuilder.com** لتحويل ملف الـ PWA هذا مباشرة إلى ملف `AAB` أو `APK` ورفعه للمتجر بلمح البصر دون كتابة سطر كود واحد!

### الخيار الثاني: بناء تطبيق Native كامل باستخدام CapacitorJS (موصى به للأداء الأقصى)
لقد قمنا بتثبيت وإعداد **Capacitor** ليتيح لك فتح كود التطبيق في برنامج **Android Studio** وبناء تطبيق حقيقي بالكامل.

---

## 🛠️ خطوات بناء تطبيق Android محلياً (APK/AAB) خطوة بخطوة

بما أن بناء التطبيق بصيغة `APK` يتطلب بيئة الأندرويد المحلية (Android SDK & Java)، يمكنك القيام بذلك بسهولة على جهاز الكمبيوتر الخاص بك باتباع الخطوات التالية:

### 1️⃣ تصدير الكود من منصة AI Studio
1. اذهب إلى قائمة **Settings** (الإعدادات) في أعلى اليمين في AI Studio.
2. اضغط على **Export to ZIP** لتحميل ملف المشروع كاملاً على جهازك، أو قم بربطه بـ **GitHub** وسحبه لجهازك.

### 2️⃣ تجهيز البيئة على جهاز الكمبيوتر الخاص بك
تأكد من تثبيت البرامج التالية على جهازك:
* **Node.js** (الإصدار 18 أو أحدث)
* **Android Studio** (لبناء ملف الـ APK وتوقيعه للرفع للمتجر)

### 3️⃣ تشغيل أوامر بناء تطبيق الأندرويد
افتح Terminal (موجّه الأوامر) في مجلد المشروع الذي قمت بتحميله، ونفّذ الأوامر التالية بالترتيب:

```bash
# 1. تثبيت الحزم البرمجية
npm install

# 2. بناء ملفات الواجهة الأمامية المستقرة (Production Build)
npm run build

# 3. إضافة منصة الأندرويد للمشروع (تُشغل لأول مرة فقط)
npx cap add android

# 4. مزامنة الملفات والأيقونات مع بيئة الأندرويد
npm run cap:sync
```

### 4️⃣ فتح مشروع الأندرويد في Android Studio
لتشغيل التطبيق على محاكي أو استخراج ملف الـ APK، قم بتشغيل الأمر التالي لفتح المشروع تلقائياً في Android Studio:

```bash
npm run cap:open-android
```

أو افتح **Android Studio** واختر **Open Project** ثم حدد مجلد `android` الموجود داخل مشروعك.

---

## 🎨 تخصيص الأيقونات وشاشة البدء (Splash Screen)

لتغيير أيقونة التطبيق وشاشة البدء لتناسب هويتك التجارية قبل رفعها للمتجر:
1. قم بتثبيت أداة توليد الأيقونات تلقائياً عبر تشغيل:
   ```bash
   npm install @capacitor/assets --save-dev
   ```
2. ضع أيقونة تطبيقك باسم `icon.png` وشاشة البدء باسم `splash.png` داخل مجلد يسمى `assets` في جذر المشروع.
3. قم بتشغيل الأمر التالي ليقوم بتوليد جميع مقاسات الأيقونات لكل الهواتف تلقائياً:
   ```bash
   npx capacitor-assets generate --android
   ```

---

## 🌍 ربط التطبيق بالخوادم المحلية (Local APIs) أو السحابية (Production API URL)

بما أن التطبيق سيعمل على الهواتف بشكل منفصل، يجب أن يتصل بالخادم (الخلفية):
1. **من داخل التطبيق مباشرة:** يمكنك الضغط على أيقونة الإعدادات (الترس ⚙️) في شاشة تسجيل الدخول وكتابة رابط السيرفر والمنفذ الخاص بك (مثال: `http://192.168.1.100:3000` أو `192.168.1.100:3000`).
2. **في الإعدادات الافتراضية للمشروع:** تم ضبط `capacitor.config.ts` ليعمل ببروتوكول `http` للسماح بالاتصال بالسيرفرات المحلية دون مشاكل أمان المحتوى المختلط (Mixed Content).

---

## 🔒 حل مشكلة الاتصال بسيرفر محلي (Cleartext HTTP Traffic) على الأندرويد

بشكل افتراضي، نظام أندرويد يمنع الاتصال بروابط غير مشفرة (تبدأ بـ `http://` بدلاً من `https://`). إذا كان سيرفر المطعم الخاص بك يعمل بـ `http://` (مثل بروتوكول IP محلي `http://192.168.1.50:3000`)، يجب تفعيل ميزة **Cleartext** في مشروع الأندرويد لكي يتزامن التطبيق بنجاح:

### 🛠️ الخطوات في Android Studio لتفعيل الاتصال المحلي:
1. افتح مشروع الأندرويد في **Android Studio** (عبر تشغيل `npm run cap:open-android`).
2. في القائمة الجانبية اليسرى للملفات، اذهب إلى:
   `app` > `manifests` > `AndroidManifest.xml`
3. ابحث عن وسم `<application>` (يكون عادةً في السطر 12-15).
4. أضف الخاصية التالية داخل وسم `<application>`:
   ```xml
   android:usesCleartextTraffic="true"
   ```
   **مثال لشكل الكود بعد الإضافة:**
   ```xml
   <application
       android:allowBackup="true"
       android:icon="@mipmap/ic_launcher"
       android:label="@string/app_name"
       android:roundIcon="@mipmap/ic_launcher_round"
       android:supportsRtl="true"
       android:usesCleartextTraffic="true"
       android:theme="@style/AppTheme">
   ```
5. احفظ الملف، ثم أعد بناء التطبيق وتشغيله في المحاكي أو على الهاتف المحمول الخاص بك (عبر الضغط على زر التشغيل الأخضر في Android Studio 🟩 ▶️).

---

## 🏆 الرفع على متجر Google Play
1. داخل **Android Studio**، من القائمة العلوية اختر **Build** > **Generate Signed Bundle / APK**.
2. اختر **Android App Bundle (AAB)** (وهي الصيغة المطلوبة حديثاً لمتجر جوجل بلاي).
3. قم بإنشاء مفتاح توقيع جديد (Keystore) لضمان أمان تطبيقك وموثوقيته.
4. اتبع الخطوات وسيتم حفظ ملف الـ `.aab` الجاهز للرفع مباشرة في لوحة تحكم **Google Play Console** الخاصة بك!

بالتوفيق في نشر تطبيقك الرائع على المتجر! 🌟

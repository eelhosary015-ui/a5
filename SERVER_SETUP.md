# دليل تشغيل نظام Remo Pro

## متطلبات التشغيل
1. تثبيت **Node.js** (الإصدار 18 أو أحدث) من الموقع الرسمي: [nodejs.org](https://nodejs.org)
2. تثبيت **Git** (اختياري) لتحميل الكود.

## خطوات التشغيل لأول مرة

1. **تحميل الكود:**
   قم بفك ضغط ملف المشروع في مجلد على جهاز الكمبيوتر (السيرفر).

2. **فتح التيرمينال (Terminal):**
   - افتح المجلد الذي يحتوي على ملفات المشروع.
   - اضغط بزر الماوس الأيمن في مساحة فارغة واختر "Open in Terminal" أو "Open PowerShell window here".

3. **تثبيت المكتبات:**
   اكتب الأمر التالي واضغط Enter:
   ```bash
   npm install
   ```

4. **تشغيل السيرفر:**
   اكتب الأمر التالي واضغط Enter:
   ```bash
   npm run dev
   ```
   أو للتشغيل في وضع الإنتاج (Production):
   ```bash
   npm start
   ```

## الدخول للنظام

### 1. من جهاز السيرفر (نفس الجهاز):
افتح المجلد واضغط على ملف **`تشغيل_النظام.bat`**. سيقوم الملف بتثبيت المتطلبات وتشغيل السيرفر وفتح المتصفح تلقائياً.

### 2. من الأجهزة الأخرى (الشبكة المحلية):
- يجب معرفة عنوان IP الخاص بجهاز السيرفر.
  - في ويندوز: افتح CMD واكتب `ipconfig` وابحث عن `IPv4 Address` (مثلاً: `192.168.1.10`).
- افتح المتصفح في الجهاز الآخر (موبايل، تابلت، لابتوب) واكتب:
  ```
  http://192.168.1.10:3000
  ```
  *(استبدل 192.168.1.10 بعنوان IP الخاص بجهازك)*

## إيقاف النظام
لإغلاق السيرفر بالكامل، اضغط على ملف **`إيقاف_النظام.bat`**.
